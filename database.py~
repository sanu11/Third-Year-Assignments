import sqlite3

conn = sqlite3.connect('test.db')


"""conn.execute('''CREATE TABLE QUOTE
       (ID INT PRIMARY KEY     NOT NULL,
        NAME     TEXT    NOT NULL);''')
"""
"""
conn.execute("INSERT INTO QUOTE(ID,NAME) VALUES (1, 'Prevention is better than cure' )");
conn.commit()
conn.execute("INSERT INTO QUOTE(ID,NAME) VALUES (2, 'You only live once, but if you do it right, once is enough' )");
conn.commit()
conn.execute("INSERT INTO QUOTE(ID,NAME) VALUES (3, 'There are only two ways to live your life. One is as though nothing is a miracle. The other is  as though everything is a miracle.' )");
conn.commit()
conn.execute("INSERT INTO QUOTE(ID,NAME) VALUES (4, 'Life isnt about finding yourself. Life is about creating yourself.' )");
conn.commit()
conn.execute("INSERT INTO QUOTE(ID,NAME) VALUES (5, 'Put your heart, mind, and soul into even your smallest acts. This is the secret of success.' )");
conn.commit()
conn.execute("INSERT INTO QUOTE(ID,NAME) VALUES (6, 'You are never too old to set another goal or to dream a new dream.' )");
conn.commit()
conn.execute("INSERT INTO QUOTE(ID,NAME) VALUES (7, 'It is your attitude, not your aptitude, that determines your altitude' )");
conn.commit()
conn.execute("INSERT INTO QUOTE(ID,NAME) VALUES (8, 'Be yourself; everyone else is already taken.' )");
conn.commit()
conn.execute("INSERT INTO QUOTE(ID,NAME) VALUES (9,  'In three words I can sum up everything I have learned about life: it goes on.' )");
conn.commit()
conn.execute("INSERT INTO QUOTE(ID,NAME) VALUES (10, 'Live as if you were to die tomorrow. Learn as if you were to live forever.' )");
conn.commit()
"""
conn = sqlite3.connect('test.db')
cursor = conn.execute("SELECT NAME  FROM QUOTE  WHERE ID = 3")
data = cursor.fetchone()
print data[0]

conn.close()
